---
title: Listar vagas easy-apply
full: true
_openapi:
  preload:
    - >-
      /Users/macbookpro/Documents/linkedin-job-explorer/apps/docs/public/openapi-pt-BR.json
  method: GET
  webhook: false
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >
          Recupera uma lista de vagas easy-apply do LinkedIn que correspondem às
          palavras-chave e filtros.


          ### Parâmetros de Query

          - `keywords` (string, opcional): Palavras-chave de título de vaga ou
          habilidade (ex.: `React`, `Node.js`).

          - `remote` (boolean, opcional): Se `true`, filtra por posições
          remotas.

          - `past24h` (boolean, opcional): Se `true`, restringe resultados de
          busca a vagas postadas nas últimas 24 horas.


          > [!TIP]

          > Para melhor desempenho e precisão de busca, sempre especifique
          `keywords`.
---

{/* Este arquivo foi gerado por Fumadocs. Não edite este arquivo diretamente. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" é o nome antigo da v10, isso permite ambos para compatibilidade retroativa
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="/Users/macbookpro/Documents/linkedin-job-explorer/apps/docs/public/openapi-pt-BR.json" operations={[{"path":"/api/jobs","method":"get"}]} showDescription />
    </>
  );
}
